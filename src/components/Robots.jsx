import { useState } from "react";

function Robots() {
    const [reserveRobots, reservedRobots] = useState();

    const handleChange = (e) => {
        const reserve = e.target;
    }


    return (  );
}

export default Robots;